# Flawed Generated Draft — Review Exercise

This is intentionally problematic code that looks plausible. Use it for ownership review.

Expected findings:

- unsafe `innerHTML` with user task title;
- unguarded localStorage parsing;
- monolithic responsibilities;
- duplicated event listeners risk;
- missing error handling for API failures;
- no tests;
- visual polish hides maintainability issues.
